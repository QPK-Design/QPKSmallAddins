<Window x:Class="QPKSmallAddins.SmallLinesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Small Lines Finder" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        Topmost="True">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Max Length:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="TxtMinLength" Width="100" Height="22" VerticalAlignment="Center" Margin="0,0,10,0" ToolTip="e.g. 1/2&quot; or 1'-0&quot;"/>
            <Button x:Name="BtnMeasure" Content="Measure Lines" Width="100" Height="24" Click="BtnMeasure_Click"/>
        </StackPanel>

        <DataGrid x:Name="ResultsDataGrid" Grid.Row="1" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" SelectionChanged="ResultsDataGrid_SelectionChanged"
                  HeadersVisibility="Column" AlternatingRowBackground="#FFF0F0F0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Element ID" Binding="{Binding ElementId}" Width="*"/>
                <DataGridTextColumn Header="Length" Binding="{Binding LengthDisplay}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label x:Name="LblStatus" Content="Loading..." Grid.Column="0" VerticalAlignment="Center" Padding="0"/>
            <Button x:Name="BtnDeleteAll" Content="Delete All" Width="100" Height="24" Grid.Column="1" 
                    Click="BtnDeleteAll_Click" IsEnabled="False" Background="#FFFFDDDD"/>
        </Grid>
    </Grid>
</Window>